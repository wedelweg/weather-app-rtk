import Form from "./Form";
import Weather from "./Weather";

const Data = () => {
    return (
        <div>
            <Form />
            <Weather />
        </div>
    );
};

export default Data;
